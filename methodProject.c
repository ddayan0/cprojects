#include <stdio.h>
#include <stdlib.h>
/* May not worl
 *
 */
int main(){
	int x = 1;
	while (x = 1){
		calc();
	}
}

void calc(){
	int fahr;
	fahr = scanf("%d", &fahr);
	int celsius;
	int lower = 0;
	int upper;
	upper = 300;
	fahr = lower;
	if (fahr != EOF){
		printf"%d\n", (celsius = 5 * (fahr -32) / 9);
	}
	else{
		printf("Error!\n");
		exit(0);
	
	}

}
